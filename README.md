# SemanticShelf
SemanticShelf is an NLP-powered book recommender using embeddings, vector search, and semantic retrieval to generate context-aware recommendations from book descriptions and metadata.
